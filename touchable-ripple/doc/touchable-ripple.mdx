import React from 'react';
import { mdx } from '@mdx-js/react';
import { TouchableRipple } from '~/touchable-ripple';
import { Text } from '~/typography';
import { Playground } from '@divriots/dockit-react/playground';
import { Layout } from '~/layout';

export default Layout;

# TocuhableRipple

A wrapper for views that should respond to touches. Provides a material "ink ripple" interaction effect for supported platforms (>= Android Lollipop). On unsupported platforms, it falls back to a highlight effect.

## Usage

```tsx
import * as React from 'react';
import { View } from 'react-native';
import { Text } from '@divriots/starter-reactnative-paper/typography';
import { TouchableRipple } from '@divriots/starter-reactnative-paper/touchable-ripple';

const MyComponent = () => (
  <TouchableRipple
    onPress={() => console.log('Pressed')}
    rippleColor="rgba(0, 0, 0, .32)"
  >
    <Text>Press anywhere</Text>
  </TouchableRipple>
);
```

<Playground
  scope={{ TouchableRipple, Text }}
  code={`
<TouchableRipple
  style={{ padding: '20rem'}}
  onPress={() => console.log('Pressed')}
  rippleColor="rgba(0, 0, 0, .32)"
>
  <Text style={{ width: '10rem' }}>Press anywhere</Text>
</TouchableRipple>
`}
/>
