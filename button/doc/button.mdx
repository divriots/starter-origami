import React from 'react';
import { mdx } from '@mdx-js/react';
import { Button } from '@divriots/starter-origami/button';
import { Playground } from '@divriots/dockit-react/playground';
import { Layout } from '@divriots/starter-origami/layout';

export default Layout;

# Button

A button is component that the user can press to trigger an action.

## Usage

```tsx
import * as React from 'react';
import { Button } from '@divriots/starter-origami/button';

const MyComponent = () => (
  <Button icon="camera" mode="contained" onPress={() => console.log('Pressed')}>
    Press me
  </Button>
);
```

<Playground
  scope={{ Button }}
  code={`
<Button>Default</Button> 
<Button mode="outlined">Outlined</Button> 
<Button mode="contained">Contained</Button>  
`}
/>

### Text Button

<Button style={{ margin: '0.5rem', width: '12rem' }}>Default</Button>
<Button style={{ margin: '0.5rem', width: '12rem' }} icon="camera">
  Default
</Button>
<Button style={{ margin: '0.5rem', width: '12rem' }} loading={true}>
  Default
</Button>

### Outlined Button

<Button style={{ margin: '0.5rem', width: '12rem' }} mode="outlined">
  Outlined
</Button>
<Button
  style={{ margin: '0.5rem', width: '12rem' }}
  mode="outlined"
  icon="camera"
>
  Outlined
</Button>
<Button
  style={{ margin: '0.5rem', width: '12rem' }}
  mode="outlined"
  loading={true}
>
  Outlined
</Button>

### Contained Button

<Button style={{ margin: '0.5rem', width: '12rem' }} mode="contained">
  Contained
</Button>
<Button
  style={{ margin: '0.5rem', width: '12rem' }}
  mode="contained"
  icon="camera"
>
  Contained
</Button>
<Button
  style={{ margin: '0.5rem', width: '12rem' }}
  mode="contained"
  loading={true}
>
  Contained
</Button>
