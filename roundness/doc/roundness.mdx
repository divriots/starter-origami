import React from 'react';
import { mdx } from '@mdx-js/react';
import { Button } from '@divriots/starter-origami/button';
import { Layout } from '~/layout';
import { roundness } from '@divriots/starter-origami/roundness';

export default Layout;

# Roundness

Roundness (border-radius) of common elements, such as buttons.

<Button
  mode="contained"
  style={{ width: '18rem' }}
  onPress={() => {}}
>{`Button with roundness ${4}`}</Button>
